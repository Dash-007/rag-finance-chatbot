# Additional requirements for Advanced RAG System
# (Add these to your existing requirements.txt)

# For hybrid retrieval and text analysis
scikit-learn>=1.3.0

# For better async support (if not already installed)
asyncio

# Core dependencies (should already be installed)
langchain>=0.1.0
langchain-community>=0.0.10
langchain-openai>=0.0.5
langchain-pinecone>=0.0.3
python-dotenv>=1.0.0
pinecone-client>=3.0.0
openai>=1.0.0
numpy>=1.24.0